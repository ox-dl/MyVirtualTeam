<Window
    x:Class="MyVirtualTeam.App.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyVirtualTeam.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:MyVirtualTeam.App.Views"
    Title="{Binding Title}"
    Width="800"
    Height="500"
    Icon="/Assets/Images/App_Logo.ico"
    mc:Ignorable="d">

    <DockPanel LastChildFill="True">

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File" Style="{DynamicResource AppMenuItemStyle}">
                <MenuItem Command="{Binding NewProjectCommand}" Header="New" />
                <MenuItem Command="{Binding OpenProjectCommand}" Header="Open" />
                <MenuItem Command="{Binding RecentProjectCommand}" Header="Open Recent">
                    <MenuItem Header="Empty" />
                    <MenuItem Header="Clear" />
                </MenuItem>
                <Separator />
                <MenuItem Command="{Binding SaveProjectCommand}" Header="Save" />
                <MenuItem Command="{Binding SaveProjectCommand}" Header="Save All" />
                <Separator />
                <MenuItem Command="{Binding SaveProjectCommand}" Header="Close Project" />
                <Separator />
                <MenuItem Header="Export">
                    <MenuItem Header="Project" />
                    <MenuItem Header="Event" />
                    <MenuItem Header="Sequence" />
                </MenuItem>
                <Separator />
                <MenuItem Command="{Binding SaveProjectCommand}" Header="Exit" />
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Coucou" />
            </MenuItem>
            <MenuItem Header="Machines" />
            <MenuItem Header="Events" />
            <MenuItem Header="Sequences" />
            <MenuItem Header="About" />
        </Menu>

        <ContentControl Content="{Binding CurrentView}" />

    </DockPanel>

</Window>
